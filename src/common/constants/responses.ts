import { SimpleResponse } from '../interfaces/response.interface';

export const SUCCESS_RESPONSE: SimpleResponse = {
  message: 'Success',
};

export const FAILED_RESPONSE: SimpleResponse = {
  message: 'Failed',
};